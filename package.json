{
  "name": "timeserver",
  "version": "1.0.0",
  "description": "A server that returns a given time in unix and natual language.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
